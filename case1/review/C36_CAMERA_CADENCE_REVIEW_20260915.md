# C36 摄像头采集速率复核与独立压力测试

初始时间：2026-09-15 05:55；终态更新：08:13。本文记录验证范围更正及新压力条件的最终结果，不改写旧配置的CNN周期证据。

08:13最终结果：原生六帧已完成，[独立终态门禁](../logs/r2_c36_camera30_native_gate_20260915a.log)通过。CNN五个完成间隔最慢6,432,428拍，**23.319344fps@名义150MHz**；九次完成采集的最慢间隔4,929,902拍，**30.426568fps**。九个源帧均完成采集，14次显示/8,601,600像素正确，零下溢/显示缺帧。kernel34040于08:11:03.269观察到真实退出，native_check退出0、父worker退出、私有目录清理。下面早期观察均是历史快照，不是当前待完成项。

07:07早期观察：新配置已完成2个CNN帧，完成周期11,032,449和17,343,395；首个完成间隔6,310,946拍，按名义150MHz为23.768227fps。第二帧自身计算6,310,944拍，第三帧从tag2于17,343,396启动。仍未取得五个间隔，不能把这个单间隔当最终最慢fps或完整PASS。

06:54早期观察：新配置首个640×480 CNN帧已完成，18项图提交，实际计算6,101,059拍、读755,765 beat、写662,400 beat；其开始周期4,931,389、完成周期11,032,449，下一CNN已于11,032,450从tag1启动。仍需其余五帧及显示收尾，不把单帧计算耗时当稳态fps。

06:49早期观察：同一原生kernel34040仍在运行，已观测tag0/tag1完成采集，周期分别4,931,388与9,860,550；第一个完成间隔4,929,162拍，按名义150MHz为30.431136fps，距5,000,000拍上限尚余70,838拍。tag1未按旧分频跳过，首个CNN已从完成的tag0输入启动。**这只证明首个完成间隔，不是全部采集或六帧整机通过。**

06:36进度：原Mosaic六帧及稳定候选短回归已实际完成、清理并通过各自独立终态检查。新配置的原生xsim已于06:32:29启动；真实xvlog日志指向私有camera_fixture副本，真实xelab参数为`FRAME_DIVISOR=1`、640×480、6帧、1920×1080源、18项图。kernel34040于06:32:30.9939498启动，实际句柄核验Job外、亲和掩码3、BelowNormal。[内核预算证据](../logs/c36_camera30_native_kernel_budget_20260915a.log)。尚无新采集配置的最终PASS或帧率结论。

## 发现与范围

现有 testbench 的核心周期为 6,666 ps、摄像头像素对时钟周期为 14,286 ps。源帧周期 1,196,052 个摄像头时钟，`FRAME_DIVISOR=2`。因此，把它简称为“60 fps 源、隔帧采集 30 fps”不够准确。

| 指标 | 实际旧配置 / 已测证据 | 新的独立配置 |
| --- | --- | --- |
| 源帧周期（摄像头时钟） | 1,196,052 | 2,300,000 |
| 采集分频 | 2 | 1，每个源帧都应采集 |
| 源帧率，按名义核心 150 MHz 归一化 | 58.518861 fps | 30.431131 fps |
| 应采集帧率，同一时间基准 | 29.259430 fps | 30.431131 fps |
| 实际完成采集的最慢间隔 | Starry B：5,126,970 核心周期，29.257047 fps | 稳定Mosaic：4,929,902拍，30.426568fps，8个间隔全部达标 |
| CNN 六帧中五个完成间隔的最慢值 | Starry B：6,396,012 核心周期，23.452114 fps | 稳定Mosaic：6,432,428拍，23.319344fps，5个间隔全部达标 |

旧配置的 23.452 fps CNN 结果仍然有效；它不是严格“采集 ≥30 fps”压力下的证明。以仿真时间直接计算时，旧应采集率为 29.262356 fps、新率为 30.434174 fps；微小差别来自核心时钟的皮秒取整，不能混用时间基准来跨过门槛。[真实旧日志独立审计](../outputs/c36_legacy_camera_cadence_20260915a/cadence.json)和[重新执行的严格原生门禁](../logs/r2_c36_b_camera_audit_gate_20260915a.log)均保留。

## 新配置怎样验证

保持 1920×1080 RGB2 源、每行 106 个摄像头时钟的消隐、独立时钟、ROI/Resize、640×480 CNN、DDR BFM 和 CPU AXI 代理不变。只在私有 testbench 副本改变两处周期常量，增加垂直前消隐；通过实际 xelab 参数令 `FRAME_DIVISOR=1`。该参数是编译/展开配置，不是新增运行时 CSR。生产计算 RTL、模型权重均不作改写。

选择约 30.43 fps 而不是仅略高于 30 fps，是为实际采集完成的排队抖动保留约 70,837 个核心周期余量。该波形是明确的行为级压力条件，不是已经测得的 SC431 或 CSI/D-PHY 输出时序；新垂直消隐也不能当作现成传感器寄存器配置。

后检查要求：

- 实际编译记录指向私有副本，保留的副本与两常量变体逐行一致；48 个生产源文件和两份模型计划照常核验。
- 实际 xelab 的分频参数、实际源 SOF 间隔和核心/摄像头时钟映射与所选配置一致。
- 每个应采集源帧都具有真实申请、完成结果、正确像素和提交顺序，不允许用跳帧隐藏压力。
- 同时检查所有观测采集完成间隔 ≤5,000,000 核心周期，以及六帧 CNN 的五个完成间隔 ≤10,000,000 核心周期；前者对应 30 fps，后者对应 15 fps。
- 保留原先的逐层数值、内部 DW、DDR、CPU、帧所有权和显示检查。输入标称速率满足目标但实际完成间隔超限，仍拒绝通过。

## 当前执行链与证据

严格串行链为：原 Mosaic 六帧 → 稳定 Mosaic 短回归 → 稳定 Mosaic 新采集配置六帧。第三项是新独立运行 `c36_mosaic_stable_camera30_20260915a`，05:52 由 WMI 创建隐藏 worker 18740，启动时间 `2026-09-15T05:52:33.8411381+08:00`。它持有前序 worker 26392 的真实句柄，等待其退出并清理，再通过实际 `small` 终态检查；没有把短回归伪称为前序原生六帧。随后自身仍完整执行矩阵、小图及原生六帧，不复用“参数换名”的旧仿真结果。[实际句柄/Job 归属复核](../logs/c36_camera30_queue_identity_20260915b.log)确认不在 Windows Job 中，当时仅原 Mosaic 的 xsim/xsimk 活跃。

一次沙箱内只读观察不能取得外部进程 Handle，另一次 JSON 日期解析丢失小数秒；两者都不是进程退出。没有据此终止或重启任何工作。前一次无效观察已明确标为无效，后续通过可访问的真实句柄重新检查。日期比较改为保留 DateTime 精度的转换，兼容 PowerShell 7 自动解码日期及 Windows PowerShell 字符串日期。

[配置自测](../logs/c36_camera_cadence_contract_20260915b.log)确认只变两常量并拒绝改坏的源节奏；[6 项流水线单测](../logs/c36_camera_pipeline_unit_20260915a.log)覆盖旧证据不可重标为新配置、慢完成不可被快输入掩盖、14 个前序身份/阶段/退出变异。[原 Mosaic 小图完整检查](../logs/r2_c36_mosaic_camera_legacy_gate_20260915a.log)通过，说明旧路径未被新配置替代。[独立生成的测试副本](../outputs/c36_camera30_fixture_20260915a/tb_c1_r2_fused_rgb2_host_system.sv)及元数据合计 50,037 字节；这仅证明测试条件已生成，不是新配置 RTL 已通过。

负载仍为单重型 EDA、最多两逻辑 CPU、BelowNormal、启动前至少 8 GiB 空闲内存；无波形，私有向量/编译目录只在工具真实退出后清理。不宣称热安全。此次没有新下载或 GPU 训练。

本轮训练与测试目标所需的新压力验证已完成。最终[矩阵终态](../logs/r2_c36_camera30_matrix_terminal_20260915a.log)、[原生时钟小图终态](../logs/r2_c36_camera30_small_terminal_20260915a.log)和[完整原生终态](../logs/r2_c36_camera30_native_gate_20260915a.log)均通过；矩阵/小图使用旧功能节奏，只有原生六帧承担新采集压力证明。实际CNN完成周期为[11032449,17343395,23685697,30118125,36508591,42869597]，五个间隔为[6310946,6342302,6432428,6390466,6361006]，相对15fps的10,000,000拍预算最少余3,567,572拍。采集八个间隔为[4929162,4929500,4929000,4929044,4929244,4929028,4929902,4928542]，相对30fps的5,000,000拍预算最少余70,098拍。

native xsim退出0，实际墙钟5914.280秒；native_check PID16656于08:11:03.788启动、2.562秒退出0。08:11:24核对原worker/cmd/xsim/kernel的四个PID均不存在，私有目录不存在，保留34文件/311,868字节。CPU AXI代理共91,792读/91,808写，最大并发读8/写1，FIFO峰值404/512。CNN帧选择可因处理速率低于采集速率而跳过旧帧；“九帧全部采集”不表示九帧全部经过CNN。无显示缺帧同样不等于每个显示刷新都是新推理图像。

官方DDR/CPU IP、真实传感器时序、物理CDC和板测仍是后续边界；本次是MEMORY_DIV2/COMMAND_LATENCY20/STALLS0/AW_WAIT_W2条件下五个CNN间隔和八个采集间隔的有限样本，不是无限时长或所有DDR延迟条件下的保证。

`native.cadence.json`中的`RTL_compiled=false`记录的是生成fixture当时的范围，不是持续运行状态，也不被回填；实际编译与展开分别由保留的`native_xvlog.sources.log`和`native_xelab.tail.log`证明，最终执行仍由原生终态门禁证明。
