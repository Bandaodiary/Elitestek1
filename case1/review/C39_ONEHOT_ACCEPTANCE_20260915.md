# C39 one-hot 候选：完整验证与官方平台联合接续

最终状态（2026-09-15）：本候选已通过三模型、原生六帧、量化和窗口收尾验证，提升为当前开发组合；C37保留回退。纯host实际最差6,433,652周期、标称150MHz下23.314907fps，与C37五间隔全部一致。详细原要求逐项验收及未完成的板级边界见[最终验收报告](C39_THREE_OBJECTIVES_FINAL_ACCEPTANCE_20260915.md)。以下各节保留历史状态，不再代表当前运行中。

2026-09-15 20:11 状态快照。三项目标仍在推进；本文件记录已建立且实际启动的验证链，不是全部完成声明。上一轮已有资源收益见[对照报告](C39_RESOURCE_FOLLOWUP_20260915.md)。

## 1. 当前原进程与顺序

| 阶段 | 原运行 | 当前边界 |
| --- | --- | --- |
| one-hot 全算子双回压 | `c39_onehot_fallback_20260915a` | 实际运行中；不能借用 native PASS |
| 联合接口、CPU/DDR PNR 与映射后 CDC 摘要 | `c39_onehot_acceptance_20260915a` | 等待上述完整回归及清理；未启动重工具 |
| 三模型、原生小图、稳定模型640×480六帧camera30 | `c39_onehot_model_queue_20260915a` | 等待联合资源/时序/有效引脚检查；新xsim尚未启动 |

原算子worker37320（19:58:38.0939319+08:00）、Python35176（19:58:39.2736695）、ST0 vvp7340（19:58:51.7950489）；20:10核实这些原始进程仍存活，ST0最近完成作业142/187。后续两个等待worker分别21096（20:05:08.1490064）和37008（20:09:01.7279302），均Job=false。PID必须连同启动时间核验，不能按数字判断旧任务仍活着。

三个阶段串行、双核BelowNormal，准入至少8GiB空闲；后台由WMI启动并检查Windows Job隔离。只保留小摘要和来源记录，无波形。活仿真目录在进程结束后才删除，不能为了节省空间删除正在运行的vvp或输入向量。

## 2. 实际编译来源和完整算子门槛

新增[one-hot算子入口](../golden/run_c39_onehot_datapath_probe.py)，复用未弱化的C37/C39数值、回压、复位和周期检查；明确替换one-hot解包器。每轮187作业、167,635向量、925,169标量（其中367,360训练参数标量）、12种PW通道组合、48通道/1024行容量与六种模式。每个vvp墙钟上限1800秒；不是放宽RTL内部看门狗或少跑作业。

每次编译后自动读取vvp首尾各最多64KiB，验证实际文件表和STALLS参数，打印`C39_ONEHOT_COMPILED_SNAPSHOT`小JSON。ST0实测编译体约9.28MB，不复制整个文件；来源表确认新解包、native空间feeder、direct engine以及C39窄量化/窗口/计算模块，旧量化不能混入。ST1将由同一原worker顺序编译和执行。

[独立终态门槛](../golden/c39_onehot_operator_preflight.py)要求：正确原运行/启动时间及variant、两种实际编译快照、匹配的新解包路径、两次正常vvp退出、两轮完整覆盖和唯一清理标记、私有目录消失。当前运行中状态被实际拒绝；另用**内存内合成终态**验证了正例及10种证据破坏拒绝。[检查器自测小日志](../logs/c39_onehot_operator_gate_selftest_20260915a.log)。该自测没有改写任何原始记录，更不是one-hot完整RTL已通过。

## 3. 独立官方联合工程与有边界的CDC约束

建立[原六时钟资源工程](../efinity/c1_ti60_c39_joint_s2_onehot.xml)和[逐端点CDC版本](../efinity/c1_ti60_c39_joint_s2_onehot_cdc.xml)，保留真实官方S2 Lite CPU和唯一DDR、相同AXI/APB布线；不修改旧S2+native工程、它的负时序记录或失败队列。完整板级peri/pin/PLL、JTAG和DDR物理时钟关系仍未签核。

新增[约束生成/一致性检查器](../golden/c39_joint_cdc_projects.py)：

- 保留原六个时钟及核心100MHz，不将多时钟geomean当作核心Fmax。
- 从既有host提取七组逐端点5ns数据路径界限、两组Gray总线1ns skew和对应第一同步级/稳定数据束的hold例外。
- 不添加整组异步clock-group或setup false path，不切断同步器第一级到第二级的正常时序。
- 联合工程使用`FRAME_DIVISOR=1`，要求源/目的标签各32位，最低位两端必须实际存在；不能复制旧`FRAME_DIVISOR=2`工程的31位假设。
- 快照源数据从request到返回ACK保持；源/目的均为两级控制同步，不能用单纯ASYNC_REG代替数据束延迟检查。FIFO的Gray传输延迟和位间skew分别约束。

七项防止弱化约束的源码负例已通过。**32位标签、75位快照等当前仍是预期映射数量，尚无本联合工程实际get_pins结果。** 队列将运行独立STA脚本，任何缺失/多余引脚都导致失败而不是跳过。工具的完整`report_cdc`分类作为隔离的附加步骤，既有版本可能内部断言失败；该情况必须报告，不能当作物理CDC通过。

三模型等待队列额外要求联合PNR真实终态、正setup/hold、映射后标签源/目的32位和LSB两端、审计结束标记。若这些不满足，队列停止并保留原因；旧native队列不会被改写或绕过。

## 4. 新模型验证入口已准备，尚未执行

[派生器](../golden/c39_onehot_verification_sources.py)建立独立one-hot模型合同、矩阵测试、终态检查器和WMI xsim流水线。保留原C37与native运行器，使用独立`C39_ONEHOT`模型来源、快照和日志名称，不重命名旧结果冒充新验证。

已实际通过：49源闭包/50源含探针检查、无同名模块重复、one-hot真实40,476 XLR物理收益及清理/时序门槛、稳定Mosaic模型绑定、脚本解析和派生一致性。模型相关执行/融合计划继续使用对应模型；`sources()`传递模型/容量参数，不能只换解包源却偷偷使用错误计划ROM。

计划依次执行：

1. `c39_onehot_starry_matrix_20260915a`：Starry矩阵及原生时钟小图。
2. `c39_onehot_mosaic_matrix_20260915a`：Mosaic矩阵及原生时钟小图。
3. `c39_onehot_stable_camera30_20260915a`：稳定Mosaic矩阵、小图及640×480六帧、约30fps采集压力。

每项独立终态检查后才执行下一项，要求真实数值、RAM故障负控、AXI/视频证据、编译来源、原进程退出及私有目录清理。原生吞吐仍同时要求15fps和最差完成间隔不超过保留C37实测6,433,652周期。**这是纯host名义150MHz测试合同，不是官方100MHz CPU联合整机帧率承诺。** 当前没有新的one-hot原生fps结果。

## 5. 20:19：ST0终态与CPU合同覆盖补齐

one-hot原ST0 vvp7340以exit=0结束（950.781秒）：187作业、167,635向量、115,822批写、225,634参数读、六种复位，busy拒绝233,302次。原worker已自然接续ST1 vvp34108（20:14:43.2923833），20:18实际仍存活并推进至38/187；两轮尚未全部通过，私有目录必须继续保留至原进程结束。

发现此前[官方平台合同](../golden/c39_sapphire_platform_contract.py)默认只覆盖早期C37/C39两版联合顶层。新增显式host参数，默认行为不变，逐端点CDC工程前置检查现在还要求native/one-hot/one-hot CDC三版，共五版通过真实公开CPU参数、BSP、统一100MHz和校准门控检查。内存内改错CPU时钟、DDR用户时钟、校准门控、未记录的150MHz约束及压缩ISA扩展五例均被拒绝。[自测记录](../logs/c39_joint_platform_selftest_20260915a.log)。不修改BSP/企业源，不读取受保护载荷，不重编或下载CPU程序。

另确认150MHz→100MHz不能只改测试台核心半周期：原生显示采用99/200核心周期比实现74.25MHz像素事件，采集检查器也使用固定6666ps/14286ps映射及150MHz周期预算。今后验证联合100MHz吞吐时必须一起调整显示节拍、实际时钟证据及阈值；不能拿现有纯host@150MHz结果或简单频率缩放当作实际联合系统达标。

## 6. 20:28：独立映射后CDC/时序门槛接入

新增[联合CDC证据检查器](../golden/c39_joint_cdc_evidence.py)，不只读取工具PASS标记：解析实际映射FF，检查56个同步寄存器的时钟/属性/一级输入和一二级直接连接，拒绝同步级重定时；逐位核对源/目的标签；检查稳定数据束的源/目的时钟与75位快照/6位错误码。

从实际分域报告验证核心/摄像头setup与hold，逐个匹配两个方向跨域端点，并独立检查实际数据flight time小于5ns、每条路径确实受5ns Max Delay约束、Gray两组实际skew小于1ns且覆盖全部10位。新联合每帧采集预期127个camera→core和14个反向端点，不能套用旧31位标签数据。

用保留的C37实际映射/布线报告验证检查器：56个同步FF、140个跨域端点、两组Gray skew0.027/0.063ns通过；缺FIFO最高位、错误时钟、把31位旧证据套成32位、同步级间插入组合网、负slack、超时数据、错端点、漏Max Delay及超skew共九种内存内证据破坏均被拒绝。[自测小日志](../logs/c39_joint_cdc_checker_selftest_20260915a.log)。这不代表新联合工程已经通过。

新门槛已加入**尚未启动的one-hot整机流水线前置检查和独立终态检查器**，同步修改其派生器并验证一致性。三模型即使队列层看到PNR正slack，也必须通过本候选自己的实际映射后连接/路径检查才能执行。此前“源码/纯host资源检查通过”仍成立，增强后的联合完整门槛目前待真实报告；不把旧前检日志当作新门槛已通过。

Efinity运行器对C39联合工程的映射摘要只保留host CDC层次，排除名称碰巧匹配的CPU内部模块；其他既有工程的提取行为保持不变。只保留受限FF/文本报告，不复制完整网表。当前ST1原进程继续，20:27最近完成154/187，无新重工具并行。

## 7. 20:37：双回压完整通过、接口通过、联合PNR运行中

`c39_onehot_fallback_20260915a`实际complete/0，总1937.874秒；两轮均187作业/167,635向量/六种复位。ST0 busy拒绝233,302次，ST1 busy拒绝401,385次、保持192,713周期。原worker/两个vvp退出，`sim/c39_datapath_xdbsb0b6`已清理。独立终态门槛实际通过，确认两份真实one-hot编译快照；不是借用native的结果。

门槛自测改为可在运行中和完成后重复执行：完成后真实终态正例及十种证据破坏负例再次通过，旧运行中自测日志保持原样。[新终态自测](../logs/c39_onehot_operator_gate_terminal_selftest_20260915a.log)。

队列自动接续one-hot联合接口短测：9项检查及BRESP、RID、校准门控三种真实连线负控通过，私有目录清理；CPU/DDR仍为行为替身。[接口小日志](../logs/c39_resource_queue_runs/c39_onehot_acceptance_20260915a/joint_seam.stdout.log)。

真实CPU/DDR联合资源运行`c39_onehot_acceptance_20260915a_joint_s2_onehot_cdc`（worker27824，20:31:07.7368411，Job=false）已开始；MAP为**35,368 LUT4 /23,053 FF /165 RAM /125 DSP**。原PNR36484（20:33:16.5888029）仍存活，20:36 CPU累计190秒。此MAP不能与不同SDC下的旧联合数字线性相减后声称同边界收益；最终XLR、时序、有效引脚和独立路径审查尚待。

启动文件额外只读抽查：官方预置bootloader为690字节，入口所见指令均32位编码，首字节与生成RAM初始化低字节文件对应；当前MAP报告的目标筛查没有发现初始化文件缺失错误。该有限检查不能证明整个bootloader ISA兼容、实际CPU已经启动或DDR数据已加载，保持原运行边界。

## 8. 收尾条件仍未满足

双回压全算子和联合接口已完成；仍待真实联合PNR/映射后CDC、三个模型及原生吞吐。只有这些证据齐全才可决定提升获胜RTL；量化叶级、压缩短测或物理资源单项收益不能替代整个目标。官方CPU启动/调试、DDR/摄像头/显示物理链路和板测仍需按其独立边界推进。

## 9. 20:54：联合终态复核通过，模型b接续

第8节是20:37历史状态。真实联合PNR现已complete/0并清理：**51,900 XLR/165 RAM/125 DSP，最终setup/hold +0.293/+0.026ns**。独立检查实际56同步FF、127+14跨域端点、32位标签、Gray skew 0.066/0.042ns通过；核心100MHz同域setup +1.601ns。工具完整CDC分类失败，因此指定host检查通过不代表CPU/DDR PHY/JTAG/复位/板级全部CDC签核。[独立派生记录](C39_ONEHOT_JOINT_RESOURCE_CDC_REVIEW_20260915.json)。

原模型a的第一项仅在source/resource preflight失败：长顶层模块名在MAP根行被截断，旧summary漏FF并误记RAM=0，使旧检查器遇到null；没有进入host矩阵/xsim。Python和PowerShell均修复根行识别，缺失RAM不再默认为零。已通过五实际报告/四负例以及绑定原始根行的真实PowerShell函数自测；原始summary和失败状态不改写，不重跑已完成算子或布线。

新`c39_onehot_model_queue_20260915b`通过显式`-Attempt b`隔离：worker28372/start20:46:30.0731499/Job=false；第一项worker38480/start20:46:31.2396005/Job=false。全部前检通过，20:54仍执行Starry host矩阵；后续为Mosaic矩阵、stable 640×480六帧camera30。新候选实际FPS和三模型终态仍待，不借用C37旧值。

并行开展的仅为轻量源码/资源审查：新增[窗口分级选择隔离候选](../rtl/c39_window_factor/README.md)及同边界工程、短测入口，未仿真/综合、未覆盖one-hot或C37。权重TDP的“副本减半”等价于“物理RAM减半”的推断已再次排除，详见[资源探索记录](C39_RESOURCE_FOLLOWUP_20260915.md)。

20:55读取终态补正：Starry已在380.699秒complete/0，而非仍运行。四矩阵共8正确CNN帧/两实际RAM故障负控通过，原生时钟8×8 xsim和独立终态检查通过，私有目录清理。Mosaic worker33992/start20:52:55.8882325已接续；第三模型640×480六帧吞吐仍待。[Starry独立终态](../logs/c39_model_queue_runs/c39_onehot_model_queue_20260915b/c39_onehot_starry_matrix_20260915b.check.stdout.log)。

21:02：Mosaic独立终态也已通过并清理，[原始结果](../logs/c39_model_queue_runs/c39_onehot_model_queue_20260915b/c39_onehot_mosaic_matrix_20260915b.check.stdout.log)。稳定Mosaic worker19848/start20:59:23.2645836正在前置矩阵，进程31496/start20:59:32.9938692核验存活。尚未取得其640×480六帧吞吐；与低成本符号补证并行不等于叠加EDA仿真。

21:07:26核验：稳定Mosaic的矩阵、小图、原生向量、xvlog/xelab均已完成，**原生640×480六帧xsim38496/start21:06:49.2823377实际存活、Job=false**。由原worker自然接续，未重启或叠加仿真；当前没有终态数值/吞吐结果，私有目录须保留到原进程自然退出再自动清理。

21:24中途核验：实际xsimk36500/start21:06:50.6944104仍存活，CPU时间继续增长。首帧已有stage0～13共14个阶段的累计周期/输出字数与保留C37逐项相同，末项3,331,109周期；只是阶段进度，不是首帧完成或FPS证明。新增只读[小日志观察器](../scripts/observe_c39_native.ps1)，固定原PID＋启动时间核对，限定每份日志不超过1MiB，当前实际共读取约24KiB；不读mem/vvp/数据库、不启动或停止工具。不能用观察器替代六完成事件/五间隔的独立终态检查。

21:30：首个真实640×480 CNN帧已完成，tag0、18次提交、6,107,331计算周期、755,765读beat、662,400写beat、720,000生产者读取。18个阶段的累计周期和输出字数全部与保留C37首帧相同。复查测试台确认FRAME事件前持续核对每次CNN写地址/数据及生产者tag，完成时还要求全部阶段、写入量及DW包数齐备；因此这是首帧全图数值完成证据，而非只看到busy变零。第二帧已开始（job2/tag1，core cycle11,038,722）。

此时尚无五个完整帧间隔，显示运行后的DDR争用和后续帧尚待验证；不能将6,107,331首帧计算周期换成整机FPS，也不能以此提前宣布三个目标完成。原xsim和等待中的短量化worker均保留，没有重启。

22:15：实际xsimk36500/start21:06:50.6944104仍存活，已完成tag0/1/2三帧，计算周期分别6,107,331、6,316,038、6,343,352；三个完整FRAME记录与保留C37对应记录逐字相同，读写量/18提交也一致。最新运行态进度已出现4次显示完成，第4个CNN作业tag3继续运行；六帧五间隔仍未齐备。观察器新增完整帧事件计数、当前tag/stage与最后显示/采集进度，但始终不输出全量验收PASS。

同时修正文档入口，明确C37保留基线、one-hot跨目录选源、S2联合100MHz边界和旧历史记录。没有改RTL、权重、当前仿真参数或源码集合，也没有提升尚在验收的候选。
