# 赛题一架构报告：项目概览

## 2026-09-15 更新任务

沿用用户已确认的中文比赛工程报告、八节结构、单份Markdown和未完成部分留空规则。更新技术对象为C39 one-hot的49源闭包及Sapphire S2联合资源探针，旧R1顶层和22阶段网络仅作为历史对照，不再冒充当前设计。用户当前要求在两份文档完成后将仅含赛题一的整理工程推送到指定仓库；本文不进行新的EDA实验。正文仍为`chapters/赛题一_RTL架构设计与验证报告.md`，采用本次已有真实证据，不构造画质或板测结果。

创建日期：2026-08-31。

状态：用户已于 2026-08-31 确认框架，并明确允许为尚未完成的工作预留空位。

## 已明确的信息

- 文档用途：FPGA 竞赛文档的技术报告初稿，用户后续自行调整为 Word 格式。
- 文档类型：中文工程架构设计与验证报告，不按 SCI 投稿或学位论文套用章节结构。
- 技术对象：`case1` 当前图像风格迁移协处理器 RTL，以 `c1_r1_portable_soc` 为主组合顶层。
- 输出格式：单份 Markdown 报告；不生成 Word、LaTeX、PPT 或新的仿真结果。
- 技术范围：图像采集与 ISP、Resize、INT8 MicroStyle、张量存储与 AXI、帧调度、APB/IRQ、显示以及易灵思厂商适配边界。
- 板卡目标：Ti60F225I3；目前部分 Efinity 探针基于 Ti60F225/C4，必须分别说明，不能混用结论。
- 方法依据：现有 RTL、Python Golden、接口契约和已保存的定向验证与综合记录。

## 已确认的写作方案

- 暂定题目：面向 RISC-V SoC 的 FPGA 图像风格迁移协处理器架构设计与验证。
- 建议篇幅：约 6,000—8,000 中文字的正文，另附摘要、参考资料和核心 RTL 索引。
- 推荐结构：见 `outline.md`。
- 面向读者：比赛评审及需要理解系统集成方式的工程读者。
- 不增加独立文献综述章；只对确有来源的算法、接口和器件资料进行必要引用。

本次按一份独立工程报告交付，八个编号部分是报告内的小节，不拆成八份 SCI 论文章节。正文唯一文件为 `chapters/赛题一_RTL架构设计与验证报告.md`。占位采用 `【待实测补充 Pxx】` 或 `【待补充 Mxx】`，不生成模拟结果充数。

## 写作边界

报告必须区分已实现功能、已有验证结果、设计目标和后续计划。不能把 scaled 8×8 handoff、64×48 小帧回归、native 尺寸接口/替代算术通路验证、窄激励综合探针混写成 native 640×480 真实网络达到 15 fps。

默认关闭的 cache/burst/outstanding、packed-affine 等可选参数须标记为候选配置；不能仅因源码存在就描述为默认顶层已经使用。实际 Sapphire/DDR3/MIPI/HDMI IP、PLL、pin/periphery 尚待集成的部分必须与便携 RTL 分开。

本次写作不修改 RTL，不重跑 Vivado/Efinity，不读取大型波形、checkpoint 或整份历史仿真日志；默认不计算哈希或校验和。
