# 用户要求与写作决策

- 2026-08-31 用户确认：“按当前框架写，可以对尚未完成的工作预留空位”。
- 交付为一份中文工程架构报告 Markdown；用户自行转 Word。
- 允许空位覆盖板上画面、最终 I3 全系统资源/时序、native 真实 CNN、FPS、功耗、模型画质、多风格切换；不允许编造数字。
- 既有报告中不同历史层次必须剥离：ALGORITHM_SPEC 的原始专用流式目标不等于当前共享引擎；R0 三层网络不等于 R1；native echo 不等于完整真实 CNN。
- 正式正文使用连续技术论述，表格仅承担网络层表、接口、资源对比和证据边界；核心模块另有索引。
- 图形采用可直接阅读的文字结构图，并保留矢量重绘位置；不生成大型图片或额外 Word/PDF。
- 按 paper-orchestration 的独立复核要求，安排一个证据审查子任务；正文由主代理统一撰写。这不是多章 SCI 全文草拟任务，不套用一章一代理流程。

## 2026-09-16 C40 更新口径

- 当前生产基线改为 C40 host；C39 one-hot 继续表示计算子系统选源，不再代表完整采集前端版本。
- 当前 15.12909658 fps 仅属于 100 MHz、行为 CPU/DDR、640×480 六帧 xsim。官方 Sapphire CPU 实际执行和真实 DDR PHY 联合吞吐仍保留 P03 空位。
- 当前 Efinity 数值为完整 C40 host 的 41,394 XLR、129 RAM、121 DSP 与核心 setup/hold +2.046/+0.026 ns；历史 51,900 XLR 联合结果属于 S2+C39+唯一 DDR，二者不能合并。
- GitHub 发布继续排除日志、仿真临时文件、Xilinx 生成物、受保护 IP 和其他赛题，只用公开指标摘录承载结果。
