# 赛题一架构报告拟定大纲

2026-09-15更新：保持下方已确认的八节标题和叙述顺序，但以C39融合行执行架构、当前18执行阶段模型、四客户端AXI128、RGBx帧、Sapphire S2及实际六帧回归替换已过时的R1/22阶段/七客户端主线内容。RAW/ISP只作为待接入前端和已有独立模块说明；不写成当前六帧测试已覆盖真实摄像头。当前证据见新增E20—E25。未完成的板级配置、真实CPU执行、画质、功耗、FPS OSD等保留明确空位，不生成数据。

日期：2026-08-31。状态：用户已确认；允许为未完成工作保留明确占位。本文为写作大纲，非报告正文。

暂定题目：面向 RISC-V SoC 的 FPGA 图像风格迁移协处理器架构设计与验证。

## 摘要与关键词

在正文完成后撰写。概括应用目标、软硬件分工、数据流组织、定点计算方法与当前验证边界，不把目标帧率写成实测结果。

## 1 赛题需求与设计目标

交代赛题一的输入、处理、显示和 CPU 调度要求，区分竞赛指标与项目自选配置；建立功能、带宽、资源和验证约束之间的关系。

## 2 系统总体架构与软硬件分工

从输入帧到输出画面说明数据流和控制流，解释便携 RTL 与厂商 IP 分离、RISC-V 控制面与协处理器数据面分离的原因。说明主顶层及板级集成边界。

## 3 图像处理与 INT8 风格迁移算法

沿 RAW10、ISP、Resize、C8 表示、MicroStyle 分层运算、输出 RGB 的顺序说明输入、处理、输出和设计依据。给出实际采用的定点、量化、饱和与取整规则；网络层表及公式须与当前生成器和 RTL 一致。

## 4 RTL 子系统设计与模块协作

说明采集前端、ISP、参数装载、CNN engine、tensor adapter、帧任务桥及显示子系统如何协作。按任务和数据流组织论述，不将文件列表直接充作正文。

## 5 存储、总线、时钟域与帧调度

解释外部 DDR 中的 frame/tensor/parameter 区域、C8 数据排布、三输入双输出 ownership、APB START 快照、七客户端 AXI128 仲裁、ready/valid 与 CDC、abort/drain。区分默认 correctness-first 通路与可选性能通路。

## 6 仿真验证与综合资源评估

介绍 Golden—模块—子系统—SoC 的分层验证方法，整理可追溯的 Icarus、Vivado/xsim、Vivado proxy 和 Efinity map/PNR 证据。所有数值绑定尺寸、配置、目标器件和测量层级；给出当前仍未闭合的端到端测试边界。

## 7 Efinity 板级集成方案与关键问题

说明 SC431HAI/CSI、DDR3、Sapphire、HDMI/TMDS 的 wrapper 接入，统一 PLL/时钟规划及官方 Demo 的选择性参考方式。分析 15 fps 所需的访存复用、burst、多 outstanding、MAC 供数与真实 P&R 约束，不把优化模型当成硬件实测。

## 8 设计总结与后续工作

归纳当前架构特点、已取得的功能性验证证据和局限；提出下一阶段的验证与集成顺序，不宣称尚不存在的板上效果、实时性或资源闭合。

## 参考资料

只列实际使用且已核验的赛题材料、器件/IP 资料、源码与项目验证记录；文献引用与内部工程证据分开编号或分类。

## 附录：核心 RTL 与接口索引

给出与正文对应的核心模块路径、功能、上下游关系和关键接口。索引用于追溯，不替代正文的架构解释。
